VERDICT: needs-revision

### Ladder share movement contradicts the persistence prediction it is meant to test
SEVERITY: serious
QUOTE: "The share does move with the target size, and the growth ladder below is what measures it."
PROBLEM: Prediction 1 says the coverage deficit does not shrink as nT grows. The ladder is retained specifically to test this. But P6 then states the moment-term share *does* move with target size, and section 2 grounds the ladder's exemption from the gate on the premise that "the share falls as the target grows, because the moment term scales with 1/nT." If the omitted variance share itself falls along the ladder, then any coverage of the superpopulation estimand approaching nominal along the ladder would falsify Prediction 1 by construction of the gate logic — i.e. the ladder is being run on the exact trajectory that makes the share and the coverage deficit shrink together. The ladder no longer cleanly isolates "deficit persists while nT grows"; it conflates that with "share shrinks because the gate-relevant fraction shrinks," and the document never separates the two effects.
WHY IT MATTERS: The falsification criterion ("falsified if coverage of the superpopulation estimand approaches nominal along the ladder") becomes ambiguous: coverage could approach nominal because identification bias vanishes (the prediction is false) OR because the omitted-variance contribution itself shrinks with 1/nT (mechanical, says nothing about identification). The registered test cannot distinguish the prediction being false from the prediction being trivially satisfied-by-decline on the wrong quantity.
WOULD BE WRONG IF: the ladder's analysis is reported as a *contrast* between superpopulation and moment-matched coverage at each rung, so that the *gap* between those two (identification bias) is what must persist, independent of the share's absolute size. The document does not actually state this — it says "the growth ladder below is what measures it" referring to the share, not to the coverage gap. If that contrast framing is the intended analysis, it needs to be said, and the prediction's falsification criterion needs restating in terms of the gap, not raw superpopulation coverage.

---

### P3's identity "gradient gap" is offered as a correctness check but the comparison coordinates are not justified
SEVERITY: serious
QUOTE: "The gap shrinks monotonically with source size, from 0.02872 to 0.004265, which is what a correct implementation does and a wrong one does not."
PROBLEM: Two sentences earlier the document warns that "an earlier draft read a link-specific direction off these ratios and reported it as a finding. It was an artifact of comparing gradients taken in different coordinates, means and standard deviations against means and raw second moments, with no Jacobian between them." The retained identity-gap numbers come from "comparing the estimand gradient with the estimator gradient in the estimator's own coordinates, means and raw second moments." So both gradients are *expressed* in the same coordinates, but the estimand gradient at that point is $\partial\Delta/\partial m$ taken in moment coordinates while the estimator gradient is $\partial\hat\Delta/\partial m$ along the MAIC weight fit. There is no statement that these two objects *should* agree even at the identity link where MAIC is consistent: consistency is about the point estimate converging to $\Delta(F_T)$, not about the gradient of the moment-matched functional with respect to the reported-moment vector equalling the gradient of the superpopulation functional with respect to those same moments. The document presents shrinking-with-nS as obviously correct ("what a correct implementation does") without deriving that the two gradients share a limit. That is exactly the identification-vs-variance slide the document says it is avoiding.
WHY IT MATTERS: P3 is retained *as the identification probe*. If its headline "correct implementation" signature rests on an unstated assumption — that the two gradients coincide at the identity link — then the probe's calibration baseline is unverified, and the curved-link gradient differences read off the same kind of comparison cannot be trusted as identification findings either. This is the category-1 failure mode (measurement quoted supports a different claim than stated).
WOULD BE WRONG IF: at the identity link the moment-matched contrast and the superpopulation contrast differ by a term whose gradient with respect to the reported moments is $o(1)$ in source size, so the two gradients provably share a limit and the shrinkage is a correct signature. That derivation is absent; if it is in `DESIGN.md` it needs to be referenced, because the claim as written asks the reader to take on trust the very thing the probe is meant to establish.

---

### Coverage band derivation uses one-sided math for a two-sided band
SEVERITY: fatal
QUOTE: "its half-width is the multiple of the delivered Monte Carlo error, 0.004873, at which the expected number of spurious band failures across all 356 cells stays inside a registered budget of 0.1."
PROBLEM: The band is declared two-sided and the document is emphatic that "reporting only undercoverage would let a conservative method pass as correct." But the budget arithmetic, as stated, is one-sided: it bounds the expected number of spurious failures using the per-cell coverage Monte Carlo error. For *two-sided* failure (spurious exceedance in *either* direction) the spurious-failure probability per cell is roughly twice the one-sided tail at the chosen multiple. A "multiple of 0.004873 ... at which the expected spurious failures across 356 cells stays inside 0.1" computed from the one-sided quantity gives a half-width that lets through ~0.2 expected wrong-direction failures across the grid — i.e. the registered budget of 0.1 is effectively transgressed by the two-sided criterion the document insists on. Either the half-width should be set from the two-sided tail (yielding a wider band, ~0.937–0.963 style rather than 0.932–0.968), or the budget should be stated as a *per-direction* budget of 0.1. As written the band and the budget it is derived from are computed against different directional conventions.
WHY IT MATTERS: This is the registered acceptance criterion. If it is too tight in the upward direction, `maic_perturb` (which already reports B=800 width 1.012 vs reference, i.e. slightly over-wide) can fail the band for being too conservative when the band arithmetic says it should not. Conversely a method can be reported as "inside the registered band" when the derivation that produced the band assumed only one tail. The whole point of the two-sided declaration is defeated by one-sided derivation.
WOULD BE WRONG IF: the generator ran the expected-failures count as Pr(coverage − 0.95 > multiple·MCSE) + Pr(0.95 − coverage > multiple·MCSE) (i.e. already doubled), in which case the quoted half-width and budget are consistent and only the prose elides the doubling. The prose does not say this; it says "the expected number of spurious band failures," which the reader will read as already two-sided because a "failure" of a two-sided band is two-sided. The risk is that the *code* used the one-sided tail and the *prose* reads it as two-sided — exactly the generative-correct-but-sentence-wrong defect the review is asked to find. The verifier should be pointed at this specific divergence.

---

### "B = 800 is 96% to 99% of the cost" is cited twice in mutually incompatible senses
SEVERITY: serious
QUOTE: "the perturbation arm is 96% to 99% of the cost" (§2, P5) and "The perturbation arm is 96% to 99% of it" (§6, against 668.4 core-hours)
PROBLEM: Both citations point at the same registered fraction, but they are being used to support different claims. In §2 the fraction justifies B as "the study's whole budget lever." In §6 the same fraction is the denominator of a 551% cost increase: "The cost rose by 551% ... The perturbation arm is 96% to 99% of it." If the perturbation arm is 96–99% of the total, then the cost increase is dominated by B going from 200 to 800 (a 4× on the dominant component ≈ a ~3.7× to 3.96× on the total), whereas the reported rise is 551% (≈ 5.51×). The two numbers are not obviously mutually consistent: a 5.51× total rise with the perturbation arm at 96–99% throughout implies either that the non-perturbation cost also rose ~5.51×, or that the perturbation share changed between the two measurements. Neither is stated. The fraction is doing two jobs: it validates the *choice* of B (the lever) and it explains (decorates?) the *magnitude* of the overrun. The second job needs the before/after perturbation share, not the after-only share, and the document does not give the before share.
WHY IT MATTERS: The 551% figure is presented as the honest accounting of an overrun that "an earlier draft reported as a saving." If the fraction that anchors the explanation does not in fact decompose the overrun, the corrective carries less than it claims, and a reviewer reproducing the cost accounting will find the two numbers do not arithmetically compose. This is the category-1 defect: a real number used to support a claim it does not measure.
WOULD BE WRONG IF: the non-perturbation arm cost rose by ~5.51× too (e.g. the corrected `mixed` arm and extra cells added to the asymptotic arms), in which case the total rise is consistent with a constant perturbation share and the fraction is genuine. The document hints at this ("the corrected `mixed` arm added cells") but never quantifies the non-perturbation rise, so the reader cannot verify the decomposition. The fix is one sentence giving the before/after split of the 668.4 against the 102.6.

---

### "Every anchored cell falls clearly below the floor" is reported as a measured classification but the floor was redefined afterwards
SEVERITY: serious
QUOTE: "Median terms across the realized grid: omitted 0.001237, source 0.004638, target-trial 0.02394, cross -0.001462." and "every anchored cell in the realized grid falls clearly below it, and not one is even borderline."
PROBLEM: The gate's denominator was changed from `omitted / (omitted + source)` to `omitted / whole_contrast_variance`. The median terms listed show the target-trial component (0.02394) is the largest single term and the cross term is *negative* (−0.001462). With the new denominator, the share is smaller than under the old denominator almost everywhere because the denominator grew, so cells that were borderline under the old denominator mostly fall below under the new one. The document then reports, as a *finding* ("the study's first result"), that the moment term is immaterial *with an anchor* (median shares 0.0395 / 0.0100 / 0.0079 across links). But the 332-cells-clearly-above / 446-below / 14-borderline classification and the "every anchored cell is clearly below" claim are computed *after* redefining the denominator to the larger quantity. A reviewer cannot tell whether the "anchored ⟹ below floor" result is a property of anchoring or an arithmetic consequence of enlarging the denominator for the anchored cells specifically. The cross-term being *negative* in the median makes this worse: the whole-contrast-variance denominator for anchored cells includes a large positive target-trial term *and* a negative cross term, and the document does not say whether the per-cell denominators used the raw (possibly negative-contribution-adjusted) variance or a clipped positive variance. If the cross term is systematically negative for anchored cells, the denominator is larger than the identifiable variance and the share is depressed twice: once by adding the target trial's own variance, once by subtracting a negative cross term.
WHY IT MATTERS: The headline probe-phase "result" that anchoring makes the moment term immaterial is the document's first reported finding and is used (in §5) to frame `maic_entropy` as potentially closing the deficit "in these conditions." If that immateriality is partly an artifact of the denominator redefinition rather than a measurement, the finding is the same category-1 defect the review is targeting: a number generated correctly attached to a sentence it does not support.
WOULD BE WRONG IF: the classification was re-run after the denominator change (the prose implies it was: "the gate previously screened on ... leaving out ..."; "the denominator is the whole variance of the contrast ... Median terms across the realized grid"), so the floor and the classification are internally consistent. The remaining gap is the treatment of the negative median cross term inside the per-cell denominator: the document needs to state that each cell's denominator is the cell's own (positive) total variance, not a sum that could be pushed around by the median cross term. As written, the reader sees a negative median cross term and a denominator that is supposed to include it, and cannot tell whether the share math is cell-honest or median-honest.

---

### P1's order-24 tolerance check uses Monte Carlo SE that exceeds the tolerance it is validating against
SEVERITY: serious
QUOTE: "on the cloglog/mixed cell at order 24 the quadrature gives 0.73046714 against a Monte Carlo estimate of 0.73038878 with standard error 7.08e-05, 1.11 standard errors apart. P1 stops and registers no order if they ever disagree by more than three."
PROBLEM: The registered tolerance is 0.0001. The Monte Carlo reference's own standard error is 7.08e-05, i.e. 70% of the tolerance. A 3-SE agreement bar against the Monte Carlo is a bar of ~2.1e-04, which is twice the registered tolerance, so "1.11 standard errors apart" can simultaneously (i) pass the 3-SE Monte Carlo stop rule and (ii) be *outside* the registered 0.0001 tolerance (0.73046714 − 0.73038878 = 7.84e-05, which is inside 0.0001 here, but only because the discrepancy happens to land favourably; the general point holds). The criterion being used to *accept* order 24 (3-SE-from-MC) is looser than the criterion being *registered* as the tolerance (1e-04). They are different numbers measuring the same agreement, and the document does not reconcile them. It is entirely possible for a cell to be within 3 MC-SE of the quadrature and further than 1e-04 from it, in which case P1 would register an order that violates its own registered tolerance.
WHY IT MATTERS: This is the integration that produces *all* the truths every cell is compared to. A tolerance that the acceptance procedure can transgress is the same defect class as the original "48 agrees with 64" failure it is supposed to have replaced: an order is accepted because it agrees with a reference *within the reference's noise* even if the agreement exceeds the registered number. The fix the document describes (independent reference, paired check) is the right *kind* of fix but the stop rule itself re-introduces the failure mode it was meant to remove.
WOULD BE WRONG IF: the registered tolerance of 0.0001 is a *separate* check applied *in addition to* the 3-SE stop rule, and both must pass. The prose does not say this — it presents the 3-SE rule as the condition for stopping, and the 0.0001 appears earlier as the tolerance the previous (order-48) check failed by 3×. If the implementation gates on `max(discrepancy <= 1e-4 AND discrepancy <= 3·MCSE)` the finding is wrong; the document should say so, because as written the two numbers look like independent thresholds and only one is enforced.

---

### The superpopulation/moment-matched separation "as the theory requires" is asserted, not shown by a probe
SEVERITY: serious
QUOTE: "The three separate as the theory requires before any method is run. Under normal covariates MAIC's limit tracks the finite-target contrast, as a Gaussian tilt of a Gaussian must; under lognormal covariates it does not, because matching two moments no longer pins the law."
PROBLEM: This is a claim that two estimands *separate*, empirically, in a direction that constitutes "the prediction's signature" once methods are run. It is asserted from theory ("a Gaussian tilt of a Gaussian must"). But the only entity measuring the separation is the simulation itself, and the document reports no probe that quantifies the moment-matched-vs-superpopulation gap by link and shape *before* declaring separation. The §3 correction (that the middle estimand is the tilted-source contrast, not a multivariate-normal one) materially *changes* the size of the separation under `mvnorm` — under the corrected definition, tilting a Gaussian source by a linear-and-quadratic exponent returns a Gaussian, so the moment-matched contrast equals the contrast under the tilted Gaussian, which under `mvnorm` is the source law; but the *superpopulation* estimand is defined on the target law, not the tilted source. So even under `mvnorm` the three estimands need not coincide unless the target law *is* the tilted source. The document says they track "as a Gaussian tilt of a Gaussian must" but tracking is not equality, and the magnitude of the residual separation under `mvnorm` is exactly what the prediction's signature requires the analysis to detect. Without a probe reporting that residual, the registered prediction 1 is being tested against a claim ("they separate") that is currently theory-treated-as-measured.
WHY IT MATTERS: Prediction 1's falsification rests on "covering [moment-matched] well while missing the superpopulation contrast is the prediction's signature." If under `mvnorm` the two contrasts happen to agree to within Monte Carlo error, the prediction *cannot be tested* in that arm — coverage of the two will be indistinguishable and the "signature" cannot appear. The document's "under normal covariates MAIC's limit tracks the finite-target contrast" is consistent with this untestable case but framed as confirmation. A simulation whose key prediction rests on a separation it has not measured by probe is the category-3 pattern (criterion asserted rather than derived from a measured quantity). The prediction-1 test should gate on a measured under-`mvnorm` separation exceeding the coverage Monte Carlo error, or restrict the signature claim to `lognormal`/`mixed` arms.
WOULD BE WRONG IF: the analysis is *only* going to claim the signature on `lognormal`/`mixed` arms (where the document already states separation holds — "which is precisely the lognormal and mixed arms") and not on `mvnorm`, in which case the "as a Gaussian tilt of a Gaussian must" is harmless throat-clearing. But the document does not say prediction 1's signature is restricted to the non-normal arms, and section 5's framing ("If it reaches the registered coverage band across the grid ... a negative result about its own prediction") treats the grid as a whole, which would include `mvnorm` cells where the prediction may be untestable.

---

### The entropy arm's "response to the superpopulation estimand" can fail for being too wide and that failure is not in the registered band
SEVERITY: serious
QUOTE: "If it reaches the registered coverage band across the grid, the moment term suffices *in these conditions* and the study reports that as a negative result about its own prediction."
PROBLEM: The negative-result branch has a logical hole. `maic_entropy` adds the moment term to the variance. If identification bias is small *but* the added moment term is large, `maic_entropy` will *over-cover* the superpopulation estimand (interval too wide). The registered band is two-sided and would catch this — *if* the band is symmetric in its consequence. But the negative-result claim ("moment term suffices ... a negative result about its own prediction") only follows if `maic_entropy` *reaches* the band, not if it *crosses* it. A method that over-covers (from an over-large moment term compensating for a small identification bias in the other direction) would be reported as failing the two-sided band, but the document only describes the "reaches the band" branch as the positive outcome. There is no branch for "misses the band by being too conservative because the moment term is over-large on its own." That middle case — band missed upward — is exactly the case that the two-sided criterion was added to catch ("a conservative method pass as correct"), and the §5 logic does not route it.
WHY IT MATTERS: The analysis plan, as stated, has an uncovered branch. If the empirical result is "entropy over-covers," the registered interpretation does not say what that means. The natural (wrong) read is "moment term suffices and then some," which is the conservative-method-passes-as-correct failure the band was designed to prevent at the test *side* but not at the *interpretation* side. The category-5 defect: a registered analysis the software can perform but whose output has no registered interpretation.
WOULD BE WRONG IF: the analysis script explicitly reports the *direction* of band failure and labels upward misses as "moment term over-large, decomposition inconclusive," in which case the coverage is just an omission in the prose, not the plan. The document should state that branch explicitly; as written the positive branch ("reaches the band") and the partial branch ("closes part of the deficit") exhaust the registered interpretation.

---

### "Over-the-floor against 0.0791" in P6 includes MC error in a one-sided test without a stated direction
SEVERITY: serious
QUOTE: "The worst cell reaches 0.0962 including Monte Carlo error against the 0.0791 threshold, so the cross term is above the threshold and cannot be ignored."
PROBLEM: "0.0962 including Monte Carlo error" reads as point estimate + MCSE ≥ 0.0791, i.e. a one-sided upper bound. The threshold 0.0791 was solved in §2 from a coverage shift of 0.01 in either direction (the band is two-sided, the floor derivation is symmetric), so the correct comparison for "above the threshold" is whether the *lower* bound of the cross-term share exceeds 0.0791, not whether the *upper* bound does. Reporting the upper bound as the cell that "reaches" the threshold makes "above" too easy to clear: any cell whose *point estimate* is at or near 0.0791 will "reach" above it once MC error is added. If the criterion is "the cross term is too large to ignore," the relevant cell is the one whose *point estimate* (or lower confidence bound) exceeds 0.0791. The 0.0962 figure is therefore consistent with a cross term whose *true* share is well below 0.0791.
WHY IT MATTERS: This is the document's justification for carrying the cross term at all ("it is carried rather than argued away"). If the gate is one-sided-upward against a two-sided threshold, the carrying decision is supported by a comparison against the wrong reference quantity — category-1 again. The same objection applies in reverse to the gate on the moment-term share: the 332 "clearly above" cells are presumably above by margin > bootstrap SE, but the document does not say *how many SEs*, and the "borderline" class is defined as within ~1 SD ("shares from 0.0368 to 0.0675, a standard deviation of 0.0118"). The floor definition "0.01 shift" was solved exactly; the cell classification "max(share − SE) > floor for clear, |share − floor| < SE for borderline" is imposed and the bandwidth (1 SE, two-sided) differs from the criterion bandwidth (0.01 coverage shift, applied one-sided upward). These are different rules applied to the same threshold.
WOULD BE WRONG IF: "0.0962 including Monte Carlo error" actually means the point estimate is 0.0962 and MC error is the *uncertainty on that estimate* (i.e. share = 0.0962 ± MCSE), in which case the lower bound is at 0.0962 − MCSE and the prose just phrases the bound symmetrically. The prose does not parse that way ("reaches 0.0962 *including* MC error" is naturally an upper-bound reading), and the MCSE for that cell is not quoted, so the reader cannot tell. Stating the point estimate and its SE separately is the fix.

---

### The 0.824 anchored target-trial share is reported as a *result* but is a tautology of the gate
SEVERITY: minor
QUOTE: "In an anchored comparison the target trial's own effect carries a median 0.824 of the interval's variance, taken as the median of WITHIN-CELL shares across the 396 anchored cells"
PROBLEM: The §2 sentence that defines the gate's denominator ("the whole variance of the anchored contrast ... the target trial's own variance and its covariance with the reported moments, both of which are in the interval") guarantees by construction that under anchoring the target-trial variance term is large (the anchored contrast is theta_AC(m_hat) − theta_BC_hat, so theta_BC's variance is a first-order term). A median 0.824 for the target-trial share is the *consequence* of the design choice to anchor, not a measurement of magnitude that emerged from probing. Calling it "the study's first result" inflates a definitional consequence into a finding. This is mild — the underlying arithmetic is fine — but it is the same category-1 slip at lower stakes, and the document has just been corrected for exactly this framing error elsewhere (the 0.0233–0.0852 range was re-described after an earlier draft said "about 1%"). The 0.824 needs to be presented as a structural property of anchoring (which is what makes the moment term's *residual* share meaningful), not as a stand-alone empirical result.
WHY IT MATTERS: As a "result" it invites a reader to take the anchoring design as independently validated by the probe; it is actually the probe speaking the design's definition back. The honest framing is "anchoring dedicates ~0.82 of the interval budget to the target's own effect by construction, so the moment term's residual share is what is left over." Low stakes, but it is the one place the reviewer can most clearly see category-1 happening inside the document, and correcting it preempts a reviewer who will flag the pattern elsewhere.
WOULD BE WRONG IF: the 0.824 is the median of measured shares that *could have come out differently* under anchoring (e.g. because the cross term's negative contribution can in principle push the target-trial share above or below its definitional floor), in which case it is a genuine measurement. Given the cross median is −0.001462 (small relative to the target-trial median 0.02394), the variation across cells is real and the median is measured; the objection is only to the *framing as a result*, not the number. Reframe as "by construction ~0.82 with measured variation [range]" and the objection dissolves.

---

### "Falsified if coverage approaches nominal along the ladder" pins falsification on the wrong estimand
SEVERITY: serious
QUOTE: "It is falsified if coverage of the superpopulation estimand approaches nominal along the ladder."
PROBLEM: Prediction 1 has two clauses: (a) methods target the moment-matched contrast; (b) their coverage of the superpopulation estimand fails by an amount that persists as nT grows. The falsification criterion speaks only to (b)'s "fails ... persists" clause, and only through the superpopulation estimand. But the prediction's *content* is the *contrast* between moment-matched coverage and superpopulation coverage ("covering [moment-matched] well while missing the superpopulation contrast is the prediction's signature"). A design in which `maic_fixed` *also* approaches nominal along the ladder — because the moment-matched and superpopulation contrasts converge under the specific ladder cells chosen — would satisfy the falsification criterion as written (coverage of the superpopulation estimand approaches nominal) while the prediction's actual content (the two contrasts separate and only one is covered) is true. The falsification criterion tests "does superpopulation coverage close," not "do the methods cover the *wrong* quantity," which is what prediction 1 *is*. The criterion can fire on a false-positive: nominal coverage of the superpopulation estimand that arises from the two contrasts coinciding, not from the prediction failing.
WHY IT MATTERS: This is the single most important test in the study and its falsification rule tests the prediction's consequence-and-persistence clause, not its root claim about which quantity is targeted. The §3 estimand-separation discussion is exactly about the possibility that the two contrasts coincide (under `mvnorm`); if the ladder's `mvnorm` rungs happen to show nominal superpopulation coverage *because the contrasts coincide there*, the prediction is declared falsified by the registered criterion while being untestable in that arm. The criterion needs to require that the moment-matched contrast is also covered *and* the superpopulation contrast is *not*, i.e. the signature, not just superpopulation coverage closing.
WOULD BE WRONG IF: the analysis reports the moment-matched vs superpopulation coverage gap as the primary ladder statistic and only uses "superpopulation coverage approaches nominal" as a secondary flag, in which case the falsification sentence in §1 is shorthand rather than the registered rule. The §1 statement is presented as *the* falsification criterion ("That is what ... the growth ladder measures. It is falsified if coverage of the superpopulation estimand approaches nominal along the ladder"), so as written it is the rule. The fix is to restate the criterion in terms of the gap-closing-vs-persisting, which the document already has language for ("Covering it well while missing the superpopulation contrast is the prediction's signature") — the falsification clause should say "falsified if the moment-matched and superpopulation coverages converge along the ladder," not "if superpopulation coverage approaches nominal."
