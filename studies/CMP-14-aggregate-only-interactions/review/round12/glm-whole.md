VERDICT: sound

I looked hard for the shape flagged in round 11 — a true claim with a leg that cannot fail under the registered definitions, so a quarter of the claim is assertion rather than evidence. This document has one place of exactly that shape, and it is already disclosed rather than hidden. I find no fatal defect and no serious defect that is not already carried as a limitation in section 9.

The one structural shape worth naming, explicitly, since it is the shape the prompt asked for:

### Primary 1's `rank_screen` leg cannot fail, and the document says so
SEVERITY: minor
QUOTE: "`rank_screen`'s two ranges consequently overlap **by construction**, and the grid cannot falsify that leg."
PROBLEM: This is precisely the round-11 shape: primary 1 registers four legs, one of them (`rank_screen`) cannot be falsified by construction, so primary 1's "no threshold separates failing from nominal" is true for that leg and is not evidence for it. Unlike round 11, this is disclosed in section 7, the informative content is explicitly narrowed to the three CMP-14 summaries, and `R/09-smoke.R` is said to assert the structural fact.
WHY IT MATTERS: It does not invalidate the registered claim. The claim is honest about which leg carries evidence and which is structural, and the central negative result rests on the three continuous summaries.
WOULD BE WRONG IF: the narrowing to "the informative content is the three CMP-14 summaries" were not actually reflected in how primary 1's result is reported downstream, so that a reader or the verifier treated `rank_screen` as an evidence-bearing leg. The text and the `post-hoc-candidate`-style labeling on `source_survival` suggest this is handled correctly.

I checked the arithmetic the document invites checking, and it reproduces: the E1 grid is 4 states × 6 spreads × 3 discordances(ecological only, so a structural zero) × 3 budgets × 4 priors × 2 synergies(additivity only) = 504 is not read straight off the factor list, but the 72 E2 count does: per-state exported counts 8+16+24+16+8 = 72, and 8+12+8 = 28 with 72−28 = 44. The contraction false-alarm direction (removed subset alarms more often, so rate falls) is consistent with the 71/84 vs 6/169 split stated. The primary 2 tolerance aside (15 of 54 display-identical, max 0.95 vs 0.951 over 54) is internally consistent. The two "true claim, one quarter not evidence" shape is named by the document rather than smuggled.

Two clean rounds preceded this one; this one is clean. That is convergence.
