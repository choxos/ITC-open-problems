I'll read the full prompt file so the review is based on the complete protocol text, then produce the verdict and findings.VERDICT: needs-revision

No fatal finding. The registered grids, class counts, and printed arithmetic that can be checked from the text are internally consistent. Two serious defects remain, both in the family this programme keeps finding: a claim the next paragraph treats as false still standing as written, and a performance number that cannot fail under the registered definitions but is tabled as if it could.

### Placebo-arm “depends on mean and SD” still stands after the document labels that wording false
SEVERITY: serious
QUOTE: "on a curved link the arm-level value integrates the covariate distribution through \(\operatorname{expit}\), so it depends on each study's covariate mean and SD and runs from **0.2506 to 0.3760** across the registered grid."
PROBLEM: The “so” does not follow. Integrating \(\operatorname{expit}(\eta(x))\) against the covariate law makes arm prevalence depend on the whole distribution, not on the first two moments alone. Two paragraphs later the document registers within-study normality and says the same wording “is false as stated and true only once the family is fixed,” while the sentence above still asserts that wording without the family. That is the recurring withdrawn-yet-still-standing shape: the correction is present as commentary, and the defective claim is still the operative sentence.
WHY IT MATTERS: Section 2 is where E2’s DGM is defined. A reader (or a later edit that drops the historical paragraph) still has a false identification of what pins the arm-prevalence range; the round-11 repair did not remove the claim it declared false.
WOULD BE WRONG IF: The dependence sentence were explicitly restricted to the registered Normal family (or appeared only after that family is fixed and was worded as “given normality, mean and SD”), so it no longer matched the form the document itself calls false as stated.

### `rank_screen` false-alarm rate is definitionally zero, yet sits in the secondary performance table as an ordinary zero
SEVERITY: serious
QUOTE: "| `rank_screen` | existing screen | 0.0717 | 0.1951 | 0.1951 | 0 |"
PROBLEM: Under the registered three-class rule, a non-estimable coordinate has coverage in \(\{0,1\}\), never within `COVER_TOL` of 0.95, so it can never be **nominal**. `rank_screen` alarms only when the coordinate is not estimable. Therefore its false-alarm rate on the nominal denominator is **always** 0, on both arms, for any grid that uses these class definitions. The table’s E2 column “0” for `rank_screen` is not a measured specificity of a threshold rule; it is the same structural fact section 7 already concedes for primary 1’s estimability leg. The nearby caveat that a zero over twelve nominals is “weakly determined” addresses sample size for the CMP-14 rules; it does not mark `rank_screen`’s zero as non-empirical. By contrast, contraction / `target_ratio` / `eff_rank` can false-alarm on estimable nominal cells, so their zeros are real measurements.
WHY IT MATTERS: Secondary results are offered as grid-weighted diagnostic performance. Putting a definitional zero in the same false-alarm column as the CMP-14 summaries makes `rank_screen` look as well calibrated on specificity as rules that earned a zero. That is the round-11 shape again: a true reported value that cannot fail under the registered definitions, so part of the performance comparison is not evidence.
WOULD BE WRONG IF: Nominal scenarios could be non-estimable under the registered coverage definitions, or the table (or its prose) stated that `rank_screen`’s false-alarm rate is structural and not comparable to the other rows’ false-alarm rates.
