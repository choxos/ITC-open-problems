VERDICT: needs-revision

### Section 7 reports an E1 primary-3 figure beside a heading covering E2, with the wrong scenario count cited by the source it links to
SEVERITY: serious
QUOTE: "On E1, over 251 failing and 169 nominal scenarios, and on E2 over 41 and 12."
PROBLEM: Primary 1's answer sentence prints the failing/nominal counts for both arms as if they belong to primary 1. But 41 and 12 are the E2 counts cited in section 8 for E1's conclusion tested on E2 ("over 41 failing and 12 nominal scenarios"), while the E2 confounded family used by primary 3 is an 8-scenario set ("E2 | 8 | -0.5952"). A reader landing on this sentence cannot tell which experiment 41/12 come from: the section covers both arms and both primaries. The number is real but it is the section-8 bridge count relocated to the primary-1 answer row, and the earlier-stated disclosure ("section 8 establishes... over 41 failing and 12 nominal") is the only place that ties it to an experiment.
WHY IT MATTERS: Primary 1's central negative result is what the rest of the study rests on. If the 41/12 figure is read as the confounded-family size that primary 3 partitions on E2, it contradicts primary 3's own table (which says N=8 for E2). The reportable claim "every statistic overlaps, on both arms" depends on knowing which arm's denominator is being asserted; bundling a section-8 count into a section-7 sentence under a shared heading is the recurring shape of "a figure reported without saying which experiment produced it, in a section covering both."
WOULD BE WRONG IF: The two pairs of counts are each labelled by arm in the sentence and I have misread the comma structure; or the 41/12 in section 8 is actually the E2 confounded family, in which case primary 3's E2 N=8 is the error instead.

### `EFF_RATIO_OK` Ъ governance claim points at the wrong decision
SEVERITY: serious
QUOTE: "These are averages over a chosen grid and are labeled as such. ... `EFF_RATIO_OK` is the one "likelihood outweighs prior" cutoff and both rank summaries use it, at different levels. `target_ratio` compares the likelihood's marginal precision to the prior's along the target's own coordinate and alarms below it. `eff_rank` counts the eigendirections of $P_0^{-1/2} I P_0^{-1/2}$ that exceed it, and its warning then compares that count to the parameter count $p$"
PROBLEM: Section 5's threshold table registers `target_ratio` as "$\ge$ `CONTRACT_OK` 0.50 the conventional halving of the prior SD" and `eff_rank` as "$<$ the parameter count none". Both rows are silent on `EFF_RATIO_OK`: `target_ratio`'s row says it alarms below `EFF_RATIO_OK`, but the column labelled "threshold" for `target_ratio` should name `EFF_RATIO_OK = 1.00`, not `CONTRACT_OK`. The prose paragraph corrects this ("`EFF_RATIO_OK` is the one cutoff... both rank summaries use it"), but the table it sits beside still lists `CONTRACT_OK` as what `target_ratio` alarms against, which contradicts the prose. The recurring shape: a guard (the table) written next to the value it supposedly established, where the guard says something else.
WHY IT MATTERS: The threshold table is the registered link between each rule and its constant. A reader taking the table as authoritative sees `target_ratio` alarm against `0.50`, which is the contraction cutoff and a different quantity's threshold; §6 explicitly warns against exactly this confusion ("judging a contraction movement against the 0.05 coverage scale, which is a different quantity's threshold").
WOULD BE WRONG IF: The threshold-table rows are keyed by "what direction alarms" and the prose is the governing specification, in which case the table mislabels only `target_ratio`'s threshold-row value. Either way the row and the prose disagree on the constant name.

### 0.3099 fails the guard that the document says enforces 5e-5 separation from 0.3
SEVERITY: fatal
QUOTE: "the placebo arm prevalences differ: 0.3099 against 0.3141 at SD ratio 1.5, and 0.3099 against 0.3321 at SD ratio 3.0 ... `R/07-run-e2.R` computes the range over the registered grid and stops the run if any arm comes within 5e-5 of 0.3"
PROBLEM: The document motivates the 5e-5 guard as exactly the event "rounds to 0.3000 at the four decimals this document quotes" and argues the old exact-equality version was inert. But the very counterexample it then prints, 0.3099 at SD ratio 1.5, does NOT round to 0.3 at four decimals unless the cell's true value is below 0.29995 or above 0.30005; 0.3099 rounds to 0.3099. The sentence "0.2999 ... is not the failure mode" sets the failure mode as the decimals displaying "0.3000". By that definition the registered prevalences (0.3099/0.3141/0.3321) do not themselves round to 0.3, so the guard as specified would never fire on the cells this document's example uses. The claim that "5e-5 is exactly rounds to 0.3000 at the four decimals this document quotes" is a definition of the trigger event; the example cell does not satisfy the event, while the document cites the example cell as what the guard inspects.
WHY IT MATTERS: If the trigger event is "rounds to 0.3000" and no registered cell rounds to 0.3000, the guard never trips any cell and is inert in exactly the mode the old exact-equality version was inert. The repair then buys nothing, and the section's claim that the guard does work depends on a triggering prevalence that the document's own example values do not meet.
WOULD BE WRONG IF: The guard triggers on raw distance from 0.3 (|p - 0.3| <= 5e-5), not on the "rounds to 0.3000" gloss the document equates it with, in which case the prose and the code evaluate different predicate bounds and the prose gloss is the only thing wrong. The document explicitly says they are the same thing, which is the defect: "5e-5 is exactly 'rounds to 0.3000 at the four decimals this document quotes'".

### Section 9 bullet conflates the E2 r=8 sample with the section-7 r=41 cell reading
SEVERITY: serious
QUOTE: "Primary 3's rank correlation between contraction and coverage is $+0.3295$ over E1's 144 confounded scenarios and $-0.5952$ over E2's 8."
PROBLEM: The figure -0.5952 is real and is the primary-3 value reported in section 7's table (E2, N=8). But section 9 asserts it in a bullet headed "The E1 finding does not reproduce on the nonlinear arm" without saying this is primary 3 alone. Section 7 also carries, in the same prose neighbourhood, the count 41 ("On E1 ... over 251 ... and on E2 over 41 and 12" for primary 1) and the underloaded count 12 for nominal. Two experiments, four counts, two headings. The "8" used here is primary 3's; the "41" is section 8's bridge count for primary 1. Section 9's bullet cites only the 8, while section 7 prints 41 and 12 next to the line whose subject is primary 1. Reading both, a reviewer cannot reconstruct which count belongs to primary 3 without the external table.
WHY IT MATTERS: The bullet that section 7 promised to carry ("Section 7 promised this limitation would be carried here") is the primary-3 reproduction failure; if the section-9 statement inherits its count from the wrong experiment round-9's recurring shape fires.
WOULD BE WRONG IF: Section 7 makes the counts label primary unambiguously; the contradiction then reduces to the first finding.

### `curvature` run restriction says "at the first spread alone" but the design example uses ratios 1.5 and 3.0, both not "the first spread"
SEVERITY: minor
QUOTE: "the SD ratio only on `curvature`, and `curvature` runs at the first spread alone because it holds covariate means equal by construction. ... 0.3099 against 0.3141 at SD ratio 1.5, and 0.3099 against 0.3321 at SD ratio 3.0, the two ratios at which the mechanism operates."
PROBLEM: The prevalence example operates at SD ratios 1.5 and 3.0, and the section-4 text calls these "the two ratios at which the mechanism operates", implying curvature's mechanism runs at both. The grid restriction earlier said curvature runs at "the first spread alone", i.e. a single spread level (2.0) — that is a restriction on spread, not on SD ratio. The two sentences are consistent if "the first spread alone" refers only to the spread axis while SD ratio sweeps all three for curvature; but the proximity of "at the first spread alone" in section 7 and "the two ratios at which the mechanism operates" in section 4 makes the spread-level restriction read as a ratio-level one.
WHY IT MATTERS: The §4 route-counts ($8+12+8=28$) depend on curvature being spread-1 but ratio-3. Misreading the restriction as ratio-1 (which the round-9 reviewer did, per the document's own note) inverts the per-state counts. The sentence as written invites the same misread.
WOULD BE WRONG IF: Readers take "at the first spread alone" strictly as a statement about the spread axis and the SD-ratio action is governed solely by the next clause; the defect is then cosmetic imprecision rather than a contradiction.

### Secondary-figures section quotes a false-alarm rate but its "corrected" denominator's exclusion is registered nowhere
SEVERITY: serious
QUOTE: "the contraction rule's false-alarm rate falls from 0.3043 to 0.0355 under the corrected denominator, and the old value is exported alongside the new one so the size of the correction is visible."
PROBLEM: The "corrected denominator" (nominal scenarios, not !failed) is asserted as a correction in prose but is not named as the registered denominator of the false-alarm computation; the only registered rule in that neighbourhood of the document is `COVER_BAD` and `COVER_TOL` for the fail/nominal/neutral partition. The secondary computation is described as averages over a chosen grid, and the document concedes the change "flatters the diagnostics". A measurement that flatters the diagnostic and depends on a denominator registered only in prose is exploratory analysis presented with the standing of a confirmatory rule.
WHY IT MATTERS: The 0.0355 figure is the one a reader would quote; it depends on excluding the noisiest band from successes, which is a post-§6 choice. §1 itself sets the discipline: "Pre-registration exists to stop data-dependent choices from manufacturing a result." If the denominator chose what to exclude after seeing the alarm rate move, the secondary table has exactly the risk the registration is supposed to fence off.
WOULD BE WRONG IF: The nominal-scenarios denominator is computed by `state_pairs()` or another registered rule (the document does not say), in which case the prose "corrected" is right and only the registration link is missing.
