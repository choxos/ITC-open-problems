---
title: "Comparators reporting different covariates: neither the maximal nor the common adjustment set protects the ranking"
subtitle: "A simulation study answering catalog problem COV-14"
author: "Ahmad Sofi-Mahmudi"
date: 2026-09-23
bibliography: references.bib
csl: ../../../vancouver.csl
link-citations: true
number-sections: true
execute:
  echo: false
  warning: false
  message: false
format:
  gfm:
    toc: false
  pdf:
    documentclass: article
    geometry: margin=1in
    fig-pos: "H"
  odt: default
---

```{r setup}
con <- read.csv("../results/contrasts.csv"); rk <- read.csv("../results/ranking.csv")
f3 <- function(x) formatC(x, format = "f", digits = 3); f2 <- function(x) formatC(x, format = "f", digits = 2)
m <- rk[rk$pattern != "complete" & rk$strength > 0, ]
m$win <- ifelse(m$diff_int_minus_max + 1.96 * m$diff_mcse < 0, "int", ifelse(m$diff_int_minus_max - 1.96 * m$diff_mcse > 0, "max", "none"))
prim <- m[m$pattern == "adversarial" & m$similarity == "similar" & m$separation == "tied", ]
fs <- m[m$pattern == "fawsitt" & m$similarity == "similar", ]
mc <- con[con$pattern != "complete" & con$strength > 0, ]
ratio <- stats::median(abs(mc$bias_int) / pmax(abs(mc$bias_max), 1e-3))
wr <- mc$width_bnd / mc$width_max
adv1 <- con[con$pattern == "adversarial" & con$similarity == "similar" & con$separation == "tied" & con$strength == 0.5 & con$k == 1, ]
lab <- function(d) paste(sprintf("%s against %s", f3(d$p_top_max), f3(d$p_top_int)), collapse = "; ")
auc <- as.numeric(sub(".*AUROC ([0-9.]+).*", "\\1", grep("AUROC", readLines("../results/decision.md"), value = TRUE)))
```

# Abstract {.unnumbered}

**Background.** One individual-data trial is often compared, unanchored, with several
aggregate comparators that report different covariates. Adjusting each contrast for all
it reports (maximal sets) minimizes each contrast's bias; adjusting every contrast for
the covariates all comparators report (intersection) makes the omissions common.
Catalog problem COV-14 asks whether the intersection gives the better ranking.

**Methods.** MAIC of one trial against four comparators with nine covariates. Three
reporting patterns, three covariate strengths, similar or dispersed comparator
populations, separated or nearly tied true effects: 36 scenarios and 8 controls, 1000
replicates each.

**Results.** In the registered primary scenarios the maximal sets gave both lower bias
and fewer wrong top ranks (`r lab(prim)`, maximal against intersection), so the
predicted inversion was not confirmed there. It held in all six Fawsitt-like scenarios
with similar populations (for example `r lab(fs[fs$strength == 0.5 & fs$separation == "tied", ])`).
Over all 36 scenarios the intersection ranked better in `r sum(m$win == "int")` and the
maximal sets in `r sum(m$win == "max")`. The intersection's per-contrast bias was a median
`r f2(ratio)` times the maximal. The difference between the two analyses did not flag a
wrong top rank (AUROC `r f3(auc)`). A bounded analysis that lets each unreported mean
range over the other comparators' reported means covered at least
`r f3(min(mc$cover_bnd[mc$similarity == "similar"]))` with similar populations at
`r f2(min(wr))` to `r f2(max(wr))` times the maximal width, and `r f3(min(mc$cover_bnd))` with
dispersed populations.

**Conclusion.** Which set ranks better depends on whether the omission bias pushes a
lower-ranked comparator past a higher one, which is not visible in the data. Report the
maximal contrasts with the bounded analysis when comparator populations are similar.

# The problem {#sec-problem}

With independent normal covariates, MAIC [@signorovitch2010] on set $S_k$ has bias
$b_k = \sum_{m \notin S_k}(c_m + \beta_m)(\mu_{m,k} - \mu_{m,\text{IPD}})$, with $c_m$ the
prognostic coefficient and $\beta_m$ A's modification; DESIGN.md omitted $c_m$, which
matters in an unanchored comparison. A ranking depends on $b_k - b_j$. Under the
intersection this is $\sum_{m \notin S}(c_m + \beta_m)(\mu_{m,k} - \mu_{m,j})$, small when
comparator populations are similar. Under maximal sets each $b_k$ is smaller but they
differ, and the ranking breaks when a larger bias lands on a comparator with a smaller
true effect.

# Design {#sec-design}

Registered protocol: `protocol.md`; ADEMP structure [@morris2019]. Individual data on 300
patients of A, four comparators of 200, continuous outcome. Patterns: Fawsitt-like
(covariate 1 missing for comparators 2 and 3, covariates 5 to 8 for comparator 4);
random (three missing per comparator); and covariate 1, the strongest, missing for
comparator 1, which has the largest true effect. True effects separated (0.6, 0.4, 0.2,
0) or tied (0.5, 0.45, 0.2, 0). The primary scenarios were the third pattern with
similar populations and tied effects.

# Results {#sec-results}

![Probability of a wrong top rank under the intersection set against the maximal sets. Points above the diagonal favor the maximal sets.](figures/fig1-rank.png){#fig-rank}

The primary pattern was not adversarial for the top rank. Omitting covariate 1 raised
comparator 1's estimate (bias `r f3(adv1$bias_max)` at strength 0.5), widening its lead;
the exact-bias table in `results/probes.md` showed this before the run. In the Fawsitt-like
pattern the same omission raised comparators 2 and 3 past comparator 1, and the
intersection, which gives every comparator a similar bias, preserved the order
(@fig-rank). With dispersed populations the intersection's biases also diverged and
neither rule dominated.

The bounded analysis in effect imputes an unreported mean from the other comparators,
which works when they resemble each other. Maximal-set coverage fell to
`r f3(min(mc$cover_max[mc$similarity == "similar"]))` in similar scenarios where the bounded
interval kept `r f3(min(mc$cover_bnd[mc$similarity == "similar"]))` or more. Controls held:
with complete reporting the methods coincided and were unbiased; with no covariate
effects every contrast was unbiased; the simulated bias matched the exact bias within 3
MCSE in 142 of 144 contrasts, the two exceptions at 3.1 and 3.4 MCSE.

# What this does not answer {#sec-limits}

Continuous outcome and independent covariates, so non-collapsibility and correlated
omissions are absent; a single ML-NMR fit of the network and overlap as a separate factor
were not run. The ranking results depend on where omissions fall relative to the true
order, which the three patterns sample but do not exhaust. Peer review has not been
done.

# References {.unnumbered}
