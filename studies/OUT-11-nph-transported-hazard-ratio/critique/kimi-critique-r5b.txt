{"verdict":"needs-revision","summary":"The round-five rebuild fixes the right thing the right way structurally: the estimand is now the anchored Bucher difference of two study-specific least-false roots, each leg under its own baseline and censoring, with covariate adjustment held at the oracle limit and the truth pinned to a censoring-free month-scale margin. But the headline number does not survive an internal cross-check: section 5 reports leg A's censoring spread as 16.08% at kappa_A=0.30, while the verbatim code comment states that 16.08% is the value produced by the retracted target-baseline evaluation and that the correct value is 17.27% — the round-four stale-table failure recurring in this round's centerpiece fix, and a failure the 109-assertion machine check structurally cannot catch, since it verifies document-vs-export consistency, not export correctness. The prespecified ablation claims are also misstated (they omit the kappa_A=0 condition without which the document's own leg decomposition falsifies them). Citations are accurate; feasibility, the CRN design, and the coverage disclaimer are sound.","findings":[{"axis":"estimand","severity":"serious","what":"Section 5's leg decomposition reports leg A's censoring spread as 16.08% in both kappa_A=0.30 cells. The new code's own comment states that giving leg A the target baseline 'understated its censoring spread (16.08% against 17.27% measured correctly at kappa_A = 0.30, Weibull)'. Both numbers describe the same quantity in the same round; 16.08% is explicitly the value produced by the computation this round retracts. Either section 5's table was not regenerated after the baseline fix (round four's stale-table defect, recurring) or the code comment misstates the corrected pipeline. Every number derived from leg A's spread (0.62%, 17.39%, 17.88%, the 3.26/0.49 interaction increments) is under the same suspicion, and the 109-assertion check cannot adjudicate: assertions compare the document against exported values, so a stale export passes cell-by-cell while being wrong.","fix":"Rerun the E1 export end-to-end under the corrected anchored_limit, regenerate every table and prose number that depends on leg A's spread, and state explicitly in the protocol that the assertion suite proves document-export consistency only, not export correctness — then reconcile the code comment with the regenerated 16.08/17.27 pair."},{"axis":"decision-rule","severity":"serious","what":"The two prespecified ablation claims read 'gamma = 0 and kappa_B = 0 gives zero movement' and 'gamma > 0, kappa_B = 0, covariate spread collapsed to zero gives zero movement'. As stated, without a kappa_A = 0 condition, both are falsified by the document's own leg decomposition one paragraph earlier: at kappa_A = 0.30 leg A alone moves 16–17%, so the anchored contrast moves regardless of gamma and kappa_B. The computed corners (2.6e-9, 2.1e-9) are only zero because kappa_A = 0 held; the verification does not cover the claims as written. In a protocol whose claims are machine-checked and which already had a round-four decision-rule defect, the registered claim text and the verified configuration must match exactly.","fix":"Add 'kappa_A = 0' to both registered ablation claims and to their prose statements, and confirm the assertion suite checks the claims at the registered corner, not a corner inferred from context."},{"axis":"dgm-builds-in-finding","severity":"minor","what":"'Spread' is never defined in this part (max-min over the 4x4 grid? max deviation from the fixed truth? log or HR scale?). Under the natural definition (range over the independent grid), the anchored log-range is the sum of the two leg log-ranges, so the quoted 17.39% at kappa_A=kappa_B=0.30, gamma=0 implies leg B's spread at kappa_B=0.30 is at most ~1.3 points (or ~0.1 points if leg A is really 17.27%) — one to two orders of magnitude below leg A's movement at the same kappa. That asymmetry may be a genuine DGM property (beta_B solved to a small 0.75-month margin), but given finding 1 it needs an explicit leg-level table to exclude another stale or mislabeled cell.","fix":"Define 'spread' once, report leg A and leg B spreads side by side for every (kappa_A, kappa_B, gamma) cell, and if the leg-B asymmetry is real, attribute it to the solved beta_B magnitude in the protocol."},{"axis":"answers-problem","severity":"limitation","what":"E2 simulates both legs at MU_TGT with unadjusted Cox fits, i.e., the oracle-adjustment limit. The measured regime shifts (0.876/0.722 SD) and the 10.5% detectability figure are therefore bounds: any implemented MAIC (ESS loss, residual imbalance) or STC (model error) is strictly noisier and strictly less able to notice the movement. The code comment states this; section 6's prose, including the 'could an analyst notice?' paragraph, does not.","fix":"State in section 6 (and in any abstracted claim) that detectability is evaluated at the perfect-adjustment limit, so 10.5% is an upper bound on what a real analyst's test achieves."},{"axis":"decision-rule","severity":"minor","what":"The reference point and regime pairs feeding the three detectability numbers are not identified: 0.876 SD 'crossed' and 0.722 SD 'matched' do not say shift-relative-to-what (the fixed truth? the cross-regime mean?), and the 10.5% rejection rate does not say which regime pair(s) it averages over. Also '200 in the B leg' is ambiguous between per-arm and total against '250 per arm in the A leg'.","fix":"Name the reference value and the regime pair(s) behind 0.876/0.722/10.5%, and write both study sizes as per-arm or total consistently."}],"citation_problems":[],"what_is_sound":["The estimand is now the right object for OUT-11: a Bucher difference of two least-false roots, each evaluated in its own study under its own baseline hazard and censoring, with the head-to-head framing explicitly retracted and the residual non-transitivity quantified by transitivity_gap rather than assumed away.","The argument that leg A must keep the IPD study's baseline is correct for Cox-based MAIC (reweighting does not transport a baseline hazard) and for STC (the conditional model is fit in the IPD study and marginalized over target covariates); evaluating both legs at MU_TGT correctly isolates censoring dependence from adjustment error, and the residual baseline transport is reported (2.86% down to 1.38%) rather than hidden.","The truth is pinned to censoring-free objects (survival functions; a 0.75-month margin with beta_B solved per cell), so 'held exactly fixed across all sixteen' is true by construction and the conditional/marginal and realized-sample/superpopulation traps are both avoided.","E2's design is statistically careful: legs simulated once per regime and reused across crossings (two independent trials, as an indirect comparison actually has), common random numbers within a leg but never between legs (no manufactured correlation the Bucher variance assumes away), and the detectability arithmetic is internally consistent (a 0.876-SD shift against a sqrt(2)-SD test gives ~9-10% power, matching the reported 10.5%).","The coverage reporting is honest and correctly interpreted: 0.937-0.958 over 96 correlated cells is null-consistent, the max 3.01xMCSE discrepancy is about the expected maximum over 96 comparisons, and the protocol explicitly disclaims that covering the least-false parameter is not covering the estimand — which is the point of the study.","Feasibility is honest: roughly 40,000 coxph fits on n=400-500 genuinely run in minutes, and the leg-reuse design is both cheaper and more faithful.","Citations check out: Struthers & Kalbfleisch (Biometrika 1986, doi:10.2307/2336212) and Xu & O'Quigley (Biostatistics 2000, doi:10.1093/biostatistics/1.4.423) are correctly attributed and do establish the censoring- and event-weighted least-false characterization the design relies on."],"round4_resolution":[{"finding":"Head-to-head estimand (round-four fatal)","resolved":"yes","note":"anchored_limit now computes the Bucher difference of two study-specific least-false roots; transitivity_gap quantifies the gap to the direct coefficient instead of defending it."},{"finding":"Leg-A baseline choice (this round's claimed fix)","resolved":"partly","note":"The code evaluates leg A under the IPD baseline, but section 5's reported 16.08% is exactly the value the code comment attributes to the retracted target-baseline computation (correct: 17.27%); tables appear not regenerated."},{"finding":"Stale results table from deleted code (round four)","resolved":"no","note":"Same class recurred: a number produced by the retracted computation is carried as a current result, and the 109-assertion suite cannot detect export-layer staleness."},{"finding":"Decision rule declared replaced but still primary (round four)","resolved":"partly","note":"No replaced rule is visible in this part, but the two registered ablation claims are under-specified (missing kappa_A = 0), so claim text and verified configuration again diverge."},{"finding":"Replicate-count contradictions (round four)","resolved":"yes","note":"2,000 replicates, 6 covariate cells x 16 regime pairs = 96 registered pairs; internally consistent across sections 5, 6, and the code (E3 sections were not in this payload)."},{"finding":"Round-three: E2 left as prose","resolved":"yes","note":"Family (Weibull), kappa grids, gamma=0.30, beta_B via the registered margin, study sizes, regimes, and replicate counts are all registered."},{"finding":"Round-one: centerpiece was a theorem","resolved":"yes","note":"Reframed as sizing: analytic computation verified at 400,000/arm, exact ablation corners, and a finite-sample check against the E1 limit rather than a test of non-movement."}]}
