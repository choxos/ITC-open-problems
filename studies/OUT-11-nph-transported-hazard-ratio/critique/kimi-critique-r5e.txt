{"verdict":"sound","summary":"Part E survives recomputation: the four round-4 defects are verifiably repaired within the visible text, and the load-bearing arithmetic (37% noise-only decision error at SD 0.75, 15% vs 17-fold spreads, 0.0500/0.1667 constant-rule losses, 560/350/150 pooling, the classification operating characteristics) is internally consistent. The estimand work is correct: an anchored contrast with each leg's least-false coefficient under its own study's baseline, MAIC's marginal graft and STC's conditional transport treated as distinct pre-registered structural errors against a common marginal target-population Delta_RMST(18). The remaining exposures are scope, not validity: the decision-loss negative result is conditional on the registered 0.15-0.25-month margins the design itself chose, the 'every estimator' claim and the ML-NMR primary pair rest on no ML-NMR measurement, and pooled coverage certifies average rather than per-cell calibration. No checkable bibliographic citations appear in this part, and the feasibility budget is not assessable from Part E alone.","findings":[{"axis":"dgm-builds-in-finding","severity":"limitation","what":"The headline secondary finding - estimator choice moves the decision by less than sampling noise while moving the estimate 17-fold - is conditional on the registered decision margins (truth solved to sit 0.25 months above and 0.15 months below the 0.50 threshold) that the design itself chose by solving beta_B per cell. With truth 1.0+ months from the boundary, the same 0.70-0.87-month per-replicate noise would not swamp decisions. The protocol scopes the claim ('at trial-realistic sample sizes') but the margins are the operative scoping variable, not sample size.","fix":"Not fixable within the design; state it. Wherever the negative decision finding is reported (abstract, catalog entry, appendix), attach the registered margin values and the margin-to-noise ratio, so it cannot be read as 'decision metrics never discriminate'."},{"axis":"fair-comparison","severity":"minor","what":"Section 10.0's table and the sentence 'a constant rule that ignores the data outperforms every estimator' cover only the five frequentist estimators; ML-NMR was excluded from the pilot and appears nowhere in the decision-loss measurement, yet MLNMR-PH vs MLNMR-flex is registered as a primary within-row comparison and the appendix finding will presumably be stated for all methods. The claim as written overreaches the measurement, and the ML-NMR pair has no pilot evidence on its noise or bias scale.","fix":"Restrict the sentence to the five measured estimators, or add a small ML-NMR decision-loss run to the appendix before the main benchmark; at minimum register that the appendix finding is frequentist-only."},{"axis":"decision-rule","severity":"minor","what":"Pooled coverage over the 14 conditions certifies average calibration only. The operating-characteristics table is computed under homogeneous 0.95 coverage, so an estimator conservative in light-censoring cells and anti-conservative in heavy-censoring cells can pool to 0.95 and be certified 'calibrated' while being miscalibrated in both. The existing warning covers 'inconclusive' but not this direction.","fix":"Label the verdict 'pooled calibration' in the registered outcome, add the symmetric warning ('calibrated must not be read as per-cell calibration'), and report per-cell coverage descriptively even though 40 replicates make those cells individually unresolvable."}],"citation_problems":[],"what_is_sound":["The within-row paired primary comparisons (MAIC-PH vs MAIC-flex, STC-PH vs STC-flex, MLNMR-PH vs MLNMR-flex) isolate the survival-model restriction on a shared code path with no threshold, which is exactly the contrast the problem asks about; the kappa_B=0 control cells mean the studied effect is quantified rather than built in, since the PH rows are correct by construction there and the pilot confirms it (+0.021/+0.030).","Estimand handling is correct on all three classic failure modes: the outcome is marginal Delta_RMST(18) on a common target population (not a conditional effect); leg A's least-false coefficient is computed under the IPD study's baseline because reweighting patients does not transport a baseline hazard - this is the right fix and matches how the Cox least-false parameter actually depends on baseline hazard and censoring; MAIC's marginal-graft error and STC's conditional-transport error are separated and pre-registered as anticipated mechanisms rather than discovered post hoc.","D3 is honestly labeled as the performance of the PH plug-in decision procedure, not as a general HR-to-RMST conversion; applying both legs' least-false HRs to the truth's own placebo curve removes baseline noise and is if anything charitable to the plug-in, so the 1.3805-month failure range is a clean result.","The coverage classification rule now has computed operating characteristics, including the honest weak side (0.88 true coverage detected only 0.286 of the time) and the correct instruction that 'inconclusive' is not evidence of calibration; the numbers are reproducible under exact binomial intervals at n=560/350/150.","The decision-loss demolition is arithmetically sound and conservative: the constant baselines 0.0500 and 0.1667 reproduce exactly from uniform weights over six cells with margins 0.15/0.25, and the 37% noise-only error rate at SD 0.75 is correct; demoting decision metrics on this evidence while preserving the negative finding for the catalog is the right call.","The correction of the STC pilot columns is done properly: the retracted near-cancellation finding is explicitly withdrawn rather than quietly edited, the surviving non-collapsibility finding is re-registered at its corrected magnitude, and the argument for crossing kappa_B levels is not propped up on the retracted number."],"round4_resolution":[{"finding":"D1 declared replaced but still registered as primary with 0.10/0.20 cutoffs","resolved":"yes","note":"10.2b deletes D1 outright rather than demoting it, withdraws both cutoffs and the one-directional gaming claim, and 10.0/10.1 register paired, threshold-free estimation quality as primary. No verdict in this part gates on D1. Cross-references in other parts are not visible from Part E."},{"finding":"Results table carrying values from code deleted for being wrong (pre-fix STC columns)","resolved":"yes","note":"10.3 prints corrected STC values (flex now consistently near -0.05, PH non-collapsibility +0.186/+0.198), and 10.0's bias column matches the corrected table cell-for-cell, so downstream numbers are consistent with the fix. The claimed closure of the verifier gap (the uncommitted export) is delegated to section 14 and cannot be inspected from this part."},{"finding":"Replicate counts contradicting between sections","resolved":"yes","note":"40 per cell is the single registered number here; 14x40=560, 14x25=350, and 6x25=150 are all internally consistent, and the 60-replicate pilot is a separate, clearly labeled exercise rather than a conflicting count."},{"finding":"Primary cross-row comparison resting on the withdrawn matched-flexibility premise","resolved":"yes","note":"Cross-row family contrasts are demoted to descriptive in 10.1 with the retracted premise named; the primaries are within-row only, and per-replicate effective degrees of freedom are registered as the diagnostic so the flexibility gap is measured rather than assumed."}]}
