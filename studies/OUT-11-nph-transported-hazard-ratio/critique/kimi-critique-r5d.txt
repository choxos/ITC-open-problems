{"verdict":"needs-revision","summary":"The substantive repairs are real: beta_B is solved per cell so the true RMST difference sits at a registered decision margin (isolating the NPH manipulation from effect size and letting the rule fire), the gamma=0 control is an exact marginal-PH cell with an explicit anti-rigging commitment, the ipd-nph cells finally put non-proportionality on the leg MAIC/STC actually touch, and computing leg A's least-false coefficient under the IPD baseline is the correct estimand fix. However, round 4's replicate-count defect class has recurred: section 8 registers 21 conditions/840 replicates while section 9's primary-coverage arithmetic runs on 14 conditions/560 with no stated exclusion rule, and the 109-assertion machine check did not catch it because prose quantities sit outside its scope. The E1/E2 claim that the two legs' regimes are 'crossed independently over a 4x4 grid' is not verifiable as written and, on the natural reading, confounds the two legs' baseline regimes. Fix the pooling rule or the number, extend the verifier to prose numbers, and define the 4x4 before the expensive benchmark starts.","findings":[{"axis":"decision-rule","severity":"serious","what":"Section 8 registers 21 cell-by-censoring conditions at 40 replicates (840 total); section 9 computes the primary coverage standard error from '14 cell-by-censoring conditions... 560 replicates per estimator' with no stated exclusion rule. The only reading that reconciles the numbers (drop the 5 reversed-censoring and 2 margin conditions from the coverage pool) appears nowhere in the text, and 'six primary cells' (240) contradicts section 8's labeling of three rows as 'primary' (which is 9 conditions). If the exclusion is intended, the primary coverage certificate silently no longer covers reversed between-study censoring, a registered OUT-11 scenario; if 14 is stale, section 10.3's tabulated verdict probabilities are computed on the wrong n. Either way the 109 assertions did not catch it, because they bind tables to code exports while section 9's counts live in prose — the exact round-4 defect class.","fix":"State the coverage pooling rule explicitly, register it next to build_cells(), and extend review/verify-protocol.py to assert the pooled replicate counts and the derived Monte Carlo standard errors (560/240/0.009/0.014/0.048), not just the cell table."},{"axis":"answers-problem","severity":"serious","what":"E1/E2 are described as 'each leg under its own study's baseline hazard and its own censoring regime, with the two regimes crossed independently over a 4x4 grid', but the text never names the two factors or their four levels. On the natural reading (4 baseline regimes x 4 censoring regimes, each leg evaluated under its own study's version of the same 16 cells) the legs share the cell index, so the IPD leg's baseline and the aggregate leg's baseline are perfectly confounded and the design cannot attribute the transported-HR structural error to one leg versus the other — the decomposition the round-3 repair and the 'population adjustment does not transport a baseline hazard' finding depend on. If instead the legs are fully crossed, the design is 256 leg-regime pairs, not a 4x4, and the budget and replicate accounting must reflect that.","fix":"Name the two factors and their levels; state whether leg regimes are fully crossed (16x16) or paired on a shared index (16); if paired, either fully cross the leg baselines or register the confound as a stated limitation."},{"axis":"answers-problem","severity":"limitation","what":"The certified decision rule concerns coverage of RMST-difference estimators. OUT-11's question about the transported hazard ratio is answered through the exact structural-error computations (MAIC marginal graft, STC conditional transport), which carry no decision rule and no verdict-probability tabulation. A reader expecting a verdict on whether the transported HR is usable gets magnitudes, not a rule.","fix":"State in the protocol that the HR question is answered by exact error quantification only, and that the binary certify/inconclusive/fail machinery applies to the RMST estimand."},{"axis":"dgm-builds-in-finding","severity":"limitation","what":"All eight primary/ipd-nph/control/family cells share one effect margin (true Delta_RMST(18)=0.75); the only other margin (0.35) is run under balanced censoring only. Performance as a function of distance from the decision threshold is identified at two points, and at the second point under a single censoring regime.","fix":"Stated limitation is sufficient; if any claim is made about robustness across margins, add a differential-censoring condition for the margin cells."},{"axis":"dgm-builds-in-finding","severity":"minor","what":"The text claims the crossing times 'bracket the 18-month horizon'. In fact every registered crossing (conditional 6.2-14.4 months; marginal 2-14 months) precedes 18 months, so at the horizon all crossing cells are already in the reversal region; nothing brackets it.","fix":"Correct the wording to 'all crossings precede the horizon, so RMST(18) integrates both regimes'; if a post-horizon crossing is wanted for symmetry, add a cell."},{"axis":"estimand","severity":"minor","what":"One true value is registered per cell. That is fine for Delta_RMST(18), which is invariant to independent censoring, but the least-false HR-type truths (the quantities E1/E2 and the structural-error computations refer to) are follow-up-weighted and move with the censoring regime. The protocol does not state that those truths are computed per cell-by-censoring condition.","fix":"Add one sentence stating that least-false coefficient truths are recomputed within each censoring condition, and register them alongside the cell table."},{"axis":"fair-comparison","severity":"limitation","what":"The protocol declares that a method reacting to leg-level (arm-level) rather than contrast-level NPH 'will be penalized here and should not be' in the kappa_A=kappa_B cell. For the uncensored indirect contrast that stance is correct, but under differential follow-up each leg's least-false HR is follow-up-weighted, so reacting to leg-level NPH is not purely an error. Scoring this cell as must-not-penalize is an interpretive commitment.","fix":"Register the scoring stance as a declared interpretive rule tied to the uncensored target contrast, not as an objective property of the methods."},{"axis":"feasibility","severity":"minor","what":"The 109-assertion verifier enforces document-versus-code consistency, not correctness of the code's computations, and it is same-author. The registered cell properties (crossing times, marginal HR ranges, PH-test rejection rates) are themselves simulation-based estimates at 200 per arm, and their Monte Carlo error (including the denominator behind the rejection proportions) is not reported.","fix":"Report the probe replicate count and Monte Carlo error for cell_properties() outputs; consider an independent spot-check of one cell's properties by a different implementation."},{"axis":"answers-problem","severity":"minor","what":"The table introduced as 'Registered properties of every cell' lists 8 of the 10 cells; the two ipd-nph rows live in a separate earlier table that lacks the at-risk-at-18 column, so the registered properties are split across two tables with different columns.","fix":"Consolidate into one properties table with identical columns for all 10 cells."}],"citation_problems":[{"cite":"Grambsch-Therneau proportional-hazards test","problem":"None — attribution is correct (Grambsch and Therneau, 1994), and the use made of it (contrast-level PH diagnostics, calibration at the 0.05 level) matches what the test does."},{"cite":"IDN-05 (internal catalog cross-reference)","problem":"Cannot be verified from the payload or external knowledge; the claim that IDN-05 'published a maximum over eight noisy cell estimates as if it were a bound' is load-bearing for the descriptive-only demotion of per-cell coverage, so it should be covered by the same automated cross-checking as the rest of the document."}],"round4_resolution":[{"finding":"replaced decision rule still registered as primary","resolved":"partly","note":"Part D shows a single Monte Carlo CI rule with an explicit inconclusive outcome and verdict probabilities in 10.3, with no shadow of the old rule; but the primary pool it applies to is itself undefined (14 vs 21 conditions), so the registration question cannot be fully closed from this part."},{"finding":"results table carrying values from deleted code","resolved":"yes","note":"The 109-assertion verifier binds whole tables cell by cell to the code's own exports and demonstrably fired (caught the stale table when kappa_A was added). Caveat: prose quantities (560, 0.009, 0.048) are outside assertion coverage, which is why finding 1 survived."},{"finding":"replicate counts contradicting between sections","resolved":"no","note":"Same defect class recurs: section 8 registers 21 conditions/840 replicates; section 9 computes the primary coverage SE from 14 conditions/560 with no stated exclusion rule, and 'six primary cells' (240) contradicts section 8's three 'primary' rows (9 conditions)."},{"finding":"primary comparison resting on a withdrawn premise","resolved":"partly","note":"Nothing in part D rests on a premise the document retracts; the ipd-nph cells' premises (contrast-PH with NPH legs, PH test at nominal 0.040) are internally consistent and machine-checked. But the E1/E2 claim that leg regimes are 'crossed independently over a 4x4 grid' is unverifiable as written, so the presence of the round-3 repair in E1/E2 cannot be confirmed from the text."}],"what_is_sound":["Solving beta_B per cell so the true Delta_RMST(18) lands on a registered margin holds the target constant across kappa_B and is the correct way to make the decision rule able to fire; it does not build the finding in, because the gamma=0 control gives proportional methods an exact must-win cell.","The marginal-PH control is genuinely exact (0.842 at both grid ends, gamma=0), and the protocol commits in advance that if proportional methods do not win there, the comparison is rigged and the paper will say so — a real anti-rigging device.","The ipd-nph cells repair round 3 substantively: with kappa_A=0.30 the IPD-side A-vs-PBO leg that MAIC weighting and STC regression actually touch is non-proportional, and the kappa_A=kappa_B cell (proportional target contrast, strongly non-proportional legs, PH-test rejection 0.040 at nominal) tests a distinction no other cell can draw.","Computing leg A's least-false coefficient under the IPD study's baseline is the correct estimand repair — population adjustment reweights patients and does not transport a baseline hazard — and MAIC's marginal-graft error and STC's conditional-transport error are the right two structural errors to compute exactly.","All checkable Monte Carlo arithmetic is internally correct: 0.009 with n=560, 0.014 with n=240, 0.034 with n=40, and 0.048 = 0.75/sqrt(240); the planning SD of 0.75 is measured from a retained 60-replicate pilot rather than asserted.","Per-cell coverage is demoted to descriptive with the IDN-05 max-over-noisy-cells mistake explicitly not repeated, and the decision rule has an honest inconclusive outcome with verdict probabilities tabulated under calibrated and miscalibrated truths.","Crossings now occur inside the supported window (conditional 6.2-14.4 months, marginal 2-14 months), resolving round 2's divergence-without-crossing defect, and the retained violation levels are calibrated to be ones an analyst misses (78% for mild, ~40% for strong).","Estimator comparisons are paired on the replicate with common random numbers across censoring regimes, and the budget is computed from measured unit costs with machine contention recorded."]}
