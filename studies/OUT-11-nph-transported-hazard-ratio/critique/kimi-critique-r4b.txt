{"verdict":"unsound","summary":"Much of round 3 is genuinely absorbed: the config is frozen, kappa_A is a real design factor with a clever proportional-contrast ipd-nph cell, STC gets a valid conditional transport, OUT-11's partial closure is stated plainly, and the integration-order measurement is exemplary. But this round exists to catch claimed fixes the text does not contain, and there are three. The excess-over-own-variance decision metric that round 3 killed, with its pilot-tuned 0.10 cutoff, is still registered as D1 in section 10.2 with fit-for-purpose verdicts and a one-directional gaming claim. The frozen bootstrap budget of 2.0 h contradicts the protocol's own measured unit cost (250 x 350 x 0.658 s is about 16 h), and the Stan unit cost (227 s per replicate, both arms) is below the measured 296.2 s for a single 128-point fit. Sections 9 and 10.2 still quote 240-replicate standard errors after section 14 froze the design at 25 replicates per cell.","findings":[{"axis":"decision-rule","severity":"fatal","what":"Round 3's fatal finding against excess-over-own-floor is claimed 'Replaced' and the 0.10-cutoff finding claimed 'Moot', but section 10.2 retains D1 verbatim: the floor is still the error rate of a hypothetical unbiased estimator with THAT ESTIMATOR'S OWN realized SD, the 0.10/0.20 fit-for-purpose thresholds still gate verdicts, and the text still claims only that it 'cannot be gamed by an estimator that simply shrinks its variance' -- the protection round 3 confirmed covers only the harmless direction, while inflating variance still drives the excess to zero. Section 10.2 even calls D1 'The primary outcome', directly contradicting sections 10.0 and 10.1, which make paired estimation quality primary precisely because measurement showed decision metrics are unsound at these sample sizes (a constant rule scores 0.0500 against the best estimator's 0.0913). A killed metric cannot continue issuing registered verdicts.","fix":"Delete the D1 verdict thresholds, or demote D1 to an explicitly descriptive appendix with the own-SD floor replaced by a fixed floor (e.g., the registered planning SD of 0.75) so variance inflation cannot hide bias; reconcile the text so exactly one set of primary outcomes exists."},{"axis":"feasibility","severity":"fatal","what":"The frozen budget says bootstrap costs 2.0 h. The protocol's own measured unit cost says otherwise: 250 resamples x 350 replicates x 0.658 s per resample = 57,575 s, about 16.0 h. The round-3-accepted figure (11.0 h at 500 resamples) implies 5.5 h at 250, not 2.0. The 2.0 h line covers only about 44 replicates. The headline total of 24.1 h therefore understates the run by roughly 14 h, which will force exactly the unregistered mid-run cuts that round 3 ruled out and that the freeze exists to prevent.","fix":"Recompute the bootstrap line from the measured unit cost; either budget ~16 h (total ~40 h plus sensitivity arms) or register an explicit subset of cells/replicates that receives bootstrap intervals."},{"axis":"feasibility","severity":"serious","what":"The 22.1 h Stan budget rests on 227 s per replicate for BOTH ML-NMR arms at production config, yet the same table measures one 128-point flexible fit at 296.2 s. The unit number is below the cost of a single fit at the production integration order, with no reconciliation (e.g., 4-chain measurement vs 2-chain production). Given this protocol's history of false wall-clock claims, an unexplained factor-of-two unit discrepancy invalidates the budget's auditability.","fix":"State the chain count behind each measurement and re-measure the exact production configuration (2 chains, 128 points, both arms) directly."},{"axis":"feasibility","severity":"serious","what":"The 256-point integration sensitivity arm is costed at 'about 3.2 h', which is the 128-point production unit price (227 s x 50 replicates). A single 256-point flexible fit is measured at 597.1 s, so 50 replicates of the flexible arm alone cost about 8.3 h. The arm registered to measure whether any conclusion moves is understated by at least a factor of 2.6.","fix":"Cost the 256-point arm at 256-point timings."},{"axis":"decision-rule","severity":"serious","what":"Sections 9 and 10.2 are stale after the 40-to-25 replicate cut: section 9 still computes pooled coverage SE = 0.014 and pooled bias SE = 0.048 at 240 replicates, and section 10.2's D2 says 'at 240 replicates the inconclusive region is wide'. The frozen design has 150 primary replicates (SEs 0.018 and 0.061, correctly stated only in section 14). The coverage classification rule is registered in a section that misstates its own resolution.","fix":"Propagate N_REP = 25 through sections 9 and 10.2."},{"axis":"fair-comparison","severity":"serious","what":"Section 10.1 registers 'method family across rows at matched flexibility' as a primary paired comparison, but the round-2 resolution table in this same document says the matched-flexibility claim was WITHDRAWN because equal knot counts do not equate RP and M-spline flexibility. A registered primary contrast cannot rest on a premise the protocol itself retracted.","fix":"Define an explicit flexibility metric (e.g., effective degrees of freedom) for the across-row contrast, or downgrade it to descriptive pending the knot-complexity arm."},{"axis":"decision-rule","severity":"limitation","what":"At 150 primary replicates a perfectly calibrated estimator's Monte Carlo interval is roughly [0.915, 0.985], which straddles the 0.98 boundary, so the 'calibrated' verdict will be returned only about a fifth of the time and 'inconclusive' most of the time. This is disclosed, which is correct, but the protocol should state the operating characteristics of its own classification rather than only that the region is wide.","fix":"State the probability of each verdict under true calibration; no design change possible within budget."},{"axis":"feasibility","severity":"minor","what":"The refit escalation (doubled iterations at adapt_delta = 0.99, with one probe replicate showing 45 divergences) has no budget line; at the contemplated 20% rate it adds roughly 15-18 h to a frozen 24.1 h budget. 'Revised in the open' is honest but is precisely the unfrozen contingency the freeze was meant to eliminate.","fix":"Register a refit cap and its cost, with the analysis-on-passing-subset fallback already defined."}],"citation_problems":[],"round3_resolution":[{"finding":"STC stored Gauss-Hermite weights never applied","resolved":"yes","note":"verify_quad() guard registered and pilot rerun; however 10.3 does not state that its table is the post-fix rerun, and the re-registered non-collapsibility 'anticipated mechanism' (+0.383/+0.397) should be explicitly distinguished from the artifact mechanism that was withdrawn."},{"finding":"STC forced through MAIC's marginal graft","resolved":"yes","note":"STC now transports conditionally and marginalizes last; MAIC retains the graft as its own declared limitation, which is the fair treatment since the graft is MAIC's actual structure."},{"finding":"Aggregate study analyzed as IPD","resolved":"yes","note":"Covariate column deleted at generation; only reconstructed event times and reported summaries survive."},{"finding":"Excess-over-own-floor divides out variance","resolved":"no","note":"Claimed 'Replaced', but D1 in 10.2 retains the own-SD floor, repeats the one-directional gaming claim, and still issues fit-for-purpose verdicts; 10.2 also contradicts 10.0/10.1 on what is primary."},{"finding":"0.10 cutoff calibrated on the pilot it tests","resolved":"no","note":"Claimed 'Moot' because paired comparisons need no cutoff, but the cutoff still gates D1 verdicts in 10.2 and is defended in 10.3; the ML-NMR holdout protects only the rows not in the pilot."},{"finding":"kappa_A = 0 quarantined all non-proportionality on the aggregate side","resolved":"yes","note":"kappa_A is now a design factor with two ipd-nph cells, including the proportional-contrast/strongly-non-PH-arms cell, which is a genuinely good design addition."},{"finding":"Integration acceptance threshold below its own standard error","resolved":"yes","note":"Paired measurement, honest uncertainty bound (+0.024 +/- 0.023), explicit statement that absolute biases below ~0.05 months are indistinguishable from integration error, and a 256-point sensitivity arm (though the arm is mis-costed)."},{"finding":"E1/E2 and N_INT not in the locked configuration","resolved":"yes","note":"N_INT = 128 frozen in R/00-config.R; R/07-run.R refuses to start on an unfrozen config."},{"finding":"'Four chains cost the same wall clock' was false","resolved":"partly","note":"Two chains registered, but the production unit cost (227 s, both arms) is below the measured 296.2 s for a single 128-point fit and the discrepancy is never reconciled."},{"finding":"500-resample bootstrap was the dominant cost, called cheap","resolved":"no","note":"Resamples halved to 250 and a 2.0 h line added, but that line contradicts the protocol's own unit cost by about 8x (~16 h) and round 3's accepted 11 h figure by ~3x."},{"finding":"Coverage cannot be resolved at 240 replicates; no inconclusive region","resolved":"partly","note":"Inconclusive verdict registered, but sections 9/10.2 still quote the 240-replicate arithmetic after the freeze cut the design to 150 primary replicates (0.018, stated only in section 14)."},{"finding":"Prior-sensitivity and knot-complexity arms absent from runtime","resolved":"partly","note":"Both arms now costed (9.6 h total), but excluded from the 24.1 h headline 'total', and the 256-point arm is costed at the 128-point unit price."},{"finding":"No cell produced an in-window hazard crossing","resolved":"yes","note":"Crossings at 6-14 months as a side effect of solving beta_B to decision margins (section 8, other half)."},{"finding":"The study cannot settle OUT-11","resolved":"yes","note":"Section 12b states plainly that OUT-11 remains open; the one-step equivalence claim is withdrawn rather than restated."},{"finding":"Replacement decision metric killed by constant rule","resolved":"yes","note":"Decision loss correctly demoted to a secondary appendix against both constant baselines; the noise-margin arithmetic (0.70-0.87 vs 0.15-0.25) is sound and the negative result is worth reporting."}],"what_is_sound":["The integration-order analysis is the best-measured part of the protocol: paired contrasts, honest t=1.0 result for 256-vs-128, an explicit statement that absolute biases below ~0.05 months cannot be distinguished from integration error, and a reportable package-default finding (n_int = 64L biased by 0.066 months).","Section 12b's scoping is exactly right: the partial benchmark is stated, the one-step equivalence claim is withdrawn, and OUT-11 stays open.","The estimand is coherent throughout this half: target-population marginal RMST difference at a fixed 18 months, truth by converged quadrature with a guard, superpopulation summaries supplied to all methods, and D3 evaluated on the analytic least-false parameter against one fixed baseline so neither simulation nor baseline noise enters.","The ipd-nph cell with a proportional target contrast under strongly non-proportional arms is a sharp design move: it tests whether flexible methods stay valid (and what they cost) when flexibility is unneeded, with gamma's cancellation from the contrast disclosed.","Making estimation quality primary and paired on the replicate, with no threshold, is the correct response to the measured collapse of decision-loss discrimination; the retained decision-loss appendix against both constant baselines is an honestly reported negative result the catalog entry should have.","Failure handling (refit-once-and-record, analysis repeated on the all-passing subset) and per-cell checkpointing on a machine that kills long jobs are sound operational choices.","The three DOIs in this half (Hernan 2010; Aalen, Cook and Roysland 2015; Stensrud and Hernan 2020) are correctly attributed and support the narrowed novelty claim made for them."]}
