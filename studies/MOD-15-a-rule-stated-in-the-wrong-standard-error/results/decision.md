# Decision

**Refuting sentence (the rule's operating characteristics are adequate): FAILS.** Pass probability under equality 0.314 to 0.374; coverage of the pooled interval given a pass, at ecological bias up to 0.2, as low as 0.221.

Policy with the lowest worst-case RMSE over ecological bias 0 to 0.2, by configuration:

always_pool: 1; never_pool: 5

What pooling can gain: at no bias, always pooling cuts RMSE by 2% to 19% relative to never pooling; the half-SE rule's RMSE is within 0.3% of never pooling in every configuration.

